# Kiments CRM Frontend

Frontend del CRM textil construido con Next.js, Tailwind CSS y componentes base estilo shadcn.

## Desarrollo

```bash
npm run dev
```

La aplicacion abre en `http://localhost:3000` si el puerto esta libre. En esta base inicial, el login crea una sesion local temporal y redirige al modulo `Chat`.
